<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Thoughts</title>
        <!--metatags-->
        <meta charset="UTF-8">
        <meta name="author" content="Gustavo Victor">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="IE=7">
        <meta name="keywords" content="JavaScript, Thoughts, HTML, CSS, thougts, ideas, crud, login, node">
        <meta name="description" content="A website to record and show users' thoughts about anything.">

        <!--favicon-->
        <link rel="shortcut icon" href="/img/favicon.ico" type="image/x-icon" />

        <!--css-->
        <link rel="stylesheet" href="/css/style.css" type="text/css" />
    
        <!--js-->
        <script src="/js/script.js" type="text/javascript" defer></script>
    
        <!--bootstrap icons-->
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    </head>
    <body>
        <!--header-->
        <header class="header" id="main-header">
            <nav id="navbar" class="nav">
                <a href="/" id="logo" class="nav-logo">
                    <img src="/img/thoughts_logo.png" id="nav-logo-img" alt="thoughts-logo" />
                </a>
                <ul class="nav-list">
                    <li><a href="/thoughts">Thoughts</a></li>
                    <!--After authentication-->
                    {{#if session.userid}}
                        <li><a href="/thoughts/dashboard">Dashboard</a></li>
                        <li><a href="/profile">Profile</a></li>
                        <li><a href="/logout">Logout</a></li>
                    {{else}}
                        <li><a href="/login">Login</a></li>
                        <li><a href="/register">Register</a></li>
                    {{/if}}
                </ul>
                <span class="nav-icon">&equiv;</span>
            </nav>
        </header>

        <!--main-->
        <main class="container" id="main-content">
            <!--Messages-->
             {{#if messages.success}}        
                <div class="message success">
                    {{messages.success}}
                </div>
            {{/if}}

            {{#if messages.error}}        
                <div class="message error">
                    {{messages.error}}
                </div>
            {{/if}}

            <!--Pages content-->
            {{{body}}}
        </main>

        <!--modal-->
        <div class="modal-wrapper">
            <div class="modal">
                <h3 class="modal-title">Delete Account</h3>
                <p class="modal-question">Are you sure you want to delete your account permanently?</p>
                <div class="modal-buttons">
                    <form action="/profile/delete" id="delete-profile" method="post">
                        <button class="btn modal-delete-btn">Delete</button>
                    </form>
                    <button class="btn modal-cancel-btn">Cancel</button>
                </div>
            </div>
        </div>

        <!--footer-->
        <footer id="main-footer" class="footer">
            <p><span>Thoughts</span> &copy; 2024</p>
            <p>By <a href="https://github.com/Gustavo-Victor" target="_blank">Gustavo Victor</a></p>
        </footer>
    </body>
</html>